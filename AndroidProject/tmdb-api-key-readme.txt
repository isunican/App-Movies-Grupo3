To configure the TMDB API key, create a file named `secrets.properties` in the `AndroidProject` directory with the following content:
TMDB_API_KEY=<YOUR TMDB API KEY HERE>

The secrets.properties file is ignored by git, so the key is not shared